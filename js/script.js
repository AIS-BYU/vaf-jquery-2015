/**
 * PROTIP: the below DOM .ready() can be written even shorter.
 * The $ namespace will call $.ready() by default. .
 *
 * $(function(){
 *    //Your code here
 * })
 *
 */

$(document).ready(function(){
  /** DOM ready
   * All code that accesses the DOM should be in ready handler.
   **/


  /** EXERCISE 1

   Select notification
   Fade out on one of these events
   - click
   - dblclick
   - mouseenter


   Select dropdown button
   Toggle dropdown menu on click event

   **/

   /** EXERCISE 2

   Pull data from /data/customer.json
   Loop through each record to create a row
   Select the table body element
   Append that row

    */
});